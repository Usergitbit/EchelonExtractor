import { CanvasSelectorDirective } from './canvas-selector.directive';

describe('CanvasSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new CanvasSelectorDirective();
    expect(directive).toBeTruthy();
  });
});
